<%= form_with(model: business_model, local: true) do |form| %>
  <% if business_model.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(business_model.errors.count, "error") %> prohibited this business_model from being saved:</h2>

      <ul>
      <% business_model.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :business_model_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
